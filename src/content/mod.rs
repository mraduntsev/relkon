mod about;
mod brand;
mod contacts;
mod footer;
mod hero;
mod nav;
mod process;
mod projects;
mod services;
mod tick;
mod tools;

pub use crate::content::{
    about::*, brand::*, contacts::*, footer::*, hero::*, nav::*, process::*, projects::*,
    services::*, tick::*, tools::*,
};
